import React, { useState } from 'react';
import { UserCredentials } from '../../models/user';

interface Props {
    index: number;
    userCredentials: UserCredentials;
    instutionId: string;
}

function MapDetaineeComponent(props: Props) {
    return null;
}

export default MapDetaineeComponent;