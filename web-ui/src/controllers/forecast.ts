import { API } from '../api/index';

const createForecast = (jwt: string) => {

}

const getForecasts = (jwt: string) => {

}

const updateForecast = (jwt: string, forecastId: string) => {

}

const deleteForecast = (jwt: string, forecastId: string) => {

}

export default {
    createForecast,
    getForecasts,
    updateForecast,
    deleteForecast,
}